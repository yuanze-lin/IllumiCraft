__pycache__/
*.pyc

# Model weights / checkpoints (large, not meant to be pushed)
checkpoints/

# Downloaded datasets
dataset/

# Local inference output bundles
demo/single_sample_from_video_outputs/
demo/single_sample_outputs/

# Gradio demo runtime artifacts
.gradio/
gradio_outputs/

# Vestigial nested clone -- the real matanyone install used by this repo is a
# separate pip install (see Installation), not this directory.
third_party/
